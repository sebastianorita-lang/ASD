## Esercitazione 11

### Programmazione dinamica

Clonare il codice sul proprio IDE e implementare i metodi
contrassegnati da `// TODO implementare` o `// TODO implementare
ricorsivamente` delle classi

- `MatrixMultiplicationSolver`
- `LCSSolver`

Per le classi sono fornite due classi di test JUnit. La spiegazione analitica
delle soluzioni è disponibile nelle slides della parte di teoria e nel libro
Cap 15 Sezione 2.

## Consegna

Si possono fare più commit e push sul proprio repository. Dopo la prima push
l'esercitazione viene considerata consegnata.
L'ultima push viene considerata la versione finale.